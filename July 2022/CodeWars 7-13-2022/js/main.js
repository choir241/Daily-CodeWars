function countBy(x, n) {
    let z = [];
   for(let i =1;i<=n;i++){
    z.push(x*i)
   }
   return z
  }
  
  //array of positive whole number integers 
  //return first n multiples of x 
  //given number and number of times will be positive and greater than 0
  //return as an array